/** @type{import('prettier').Config} */
const config = {
  singleQuote: true,
  semi: false,
  arrowParens: 'always',
  endOfLine: 'lf',
  bracketSpacing: true,
  printWidth: 80,
  useTabs: false,
  tabWidth: 2,
  trailingComma: 'none',
  plugins: []
}

export default config
